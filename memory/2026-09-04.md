# Codebase audit

- Maintenance candidate: 4.4.2/build 87; previous uploaded beta remains 4.4.1/build 86.
- Fixed first-install settings hydration, duplicate chat hydration, fake network-setup delay, expanded native DNS-name bounds and corruption-backup error metadata. Removed unused context/animation/storage APIs and unwired screenshot-test tooling.
- Replaced the decoder advisory suppression with a scoped fixed dependency and one-line Metro consumer migration. Two build-time image-size suppressions remain because their declared fixed release is not published.
- Verification fixes cover shared Git hooks, subprocess termination, invalid ELF alignment output, version-write failures and incomplete DNS responses. A new React Doctor wrapper rejects incomplete JSON reports; warnings remain visible.
- Standards/spec reviews found shared-hook contamination and stale screenshot instructions; fixes were checked against actual Git fixtures and Xcode references. The release-doc test incorrectly coupled a source bump to an uploaded binary; it now checks consistent recorded artifact metadata independently.
- Android Debug compilation passed, but the emulator lost its package service; non-streaming install and a scoped Argent reboot also failed. Invalid recordings were removed from the candidate. iOS build/runtime and full closeout remain in progress.
- External autoreview was rejected twice by automatic approval review despite the explicit workflow request, public repository status and clean changed-file secret scan. Explicit disclosure approval is pending; no workaround was used.
- Final implementation gate passed: 137 root suites/1,049 tests and nine native suites/75 tests, with 13 native integration tests skipped. React Compiler: 142/142; completed React Doctor: 93/100, zero errors and three existing warnings. Autoreview's local dry-run preflight passed without contacting the reviewer.
- Native verification encountered severe host contention and disk exhaustion. Only inactive Android build caches from this task were removed. The iOS build is retrying serially with an Xcode result bundle; prior attempts are not counted as successful build/runtime proof.

## Takeover closeout

- User authorized taking over the inherited staged candidate; no commit, push or upload was performed. Independent app/native reviews removed remaining unused APIs, mock-only native tests and the profile export placeholder. Message-list behavioral tests now cover scheduling/cancellation, refresh and empty/skeleton states.
- An empty explicit AAB could borrow unrelated local libraries and pass alignment validation. The reproduced failing case now fails closed; valid archive controls and script fixtures pass.
- Final `verify:all` passed: 136 root suites/1,007 tests and eight native suites/68 tests. React Compiler reports 141/141; React Doctor remains 93/100 with zero errors and three warnings. These counts supersede earlier entries above.
- Current iOS Simulator Debug build and Android offline Debug build passed. iOS navigation replay passed twice unchanged; profile removal and revised network onboarding were inspected. Pixel-settling warnings remain, so no screenshot-diff/stillness claim. Stale Metro output initially showed removed source; an isolated server and cache rebuild exposed the current bundle.
- Android update hit a signing-key mismatch. A new isolated API 36 AVD preserved the existing app/data and accepted the current APK. Onboarding, recommended-settings save confirmation and chat-list navigation passed; the app process survived and the crash buffer was empty. No candidate Release archive or physical-device speed claim.
- Serialization of 100 chats/10,000 messages produced identical 1,191,381-byte output, removing 60,601 replacer callbacks and 10,200 Date constructions. Probe records Node version and load; no wall-clock improvement is inferred.
- Independent final review found one stale compiler count in this plan, corrected against final logs. Current autoreview dry-run passed input preparation, scan and isolated-engine preflight without contacting a reviewer. External P3 execution still needs the previously requested disclosure approval.
