# 2026-09-23

- 4.4.8 (93): Expo SDK 57 patch alignment after `verify:expo-doctor` drifted red (expo 57.0.22 -> 57.0.24 plus six sibling patches; transitively `@expo/cli` 57.0.26 and `babel-preset-expo` 57.0.12). The 16 open Dependabot PRs mix SDK 58 packages, React 19.3, TypeScript 7, Jest 30 and Actions majors; none were merged.
- Expo 57.0.23 added opt-in UIKit scene support (`ios.enableSceneSupport`). It is not enabled; the local SceneDelegate bridge still owns scenes. A cold restart on the simulator came back through it with history intact. Replacing the bridge with Expo's is a separate reviewed change.
- Pre-production security review (read-only, all AGENTS.md invariants traced): no Critical/High/Medium. One Info item fixed: Release kept the ATS `NSAllowsLocalNetworking` exception. The non-Debug strip build phase now deletes it; `__tests__/iosReleasePlistStrip.spec.ts` runs that phase against the real Info.plist and failed before the fix. The compiled simulator Release plist shows only `NSAllowsArbitraryLoads=false`.
- Reviews: Matt Pocock code-review (Standards: missing CHANGELOG and stale build-92 docs, both fixed; Spec: correct), autoreview through P3 scoped-clean, independent verification PASS on six frozen criteria with a negative control.
- Gates: `verify:all` on Node 24 passed 707 root and 67 native tests. On Node 26 `react-compiler-healthcheck` fails (yargs ESM), outside `engines`; run gates with `.node-version`.
- Toolchain: `xcode-select` points at Xcode 27.2 beta on this host. Builds for upload use `DEVELOPER_DIR` for stable Xcode 27.0 without changing the global selection.
- Runtime: iOS 27.0 simulator Release (onboarding, accented native reply in 1.16 s, composer empty after an autocorrected send, Logs without prompt text, About 4.4.8 (93), cold restart with decrypted history). Android 16 emulator Release APK (onboarding, native reply in 2.70 s, live rendering). The host was at load average 110-145; the first Android typing attempt hit system-wide ANRs (IME and dialer included) and was retried with paste.
