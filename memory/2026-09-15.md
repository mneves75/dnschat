# 2026-09-15

- Upgraded Expo to the latest stable patch set, `expo@57.0.22`, with 13 packages changed and the matching Expo pods. React Native stays at 0.86.3. SDK 58 is only `58.0.0-preview.1` on RN `0.88.0-rc.0`, so it was not adopted.
- Also on the dependency side: expo-doctor went to 1.20.4 (20/20 checks), the dead image-size suppressions and the expired release-age exclusions were removed, and `pnpm audit` is clean.
- A read-only security audit found no Critical, High or Medium issues. A best-practices review found 8 defects. Every confirmed item was fixed with a regression test that failed first:
  - keychain key migration to device-only;
  - length-only DNS log redaction, and log/backup purge on chat delete;
  - public resolvers removed from the allowlist;
  - backslash links rejected;
  - no shared dnsjava cache;
  - TXT UTF-8 decoding across character-strings and on the Android legacy rung;
  - typed, localized chat errors and a scoped Retry;
  - rejected sends restored to the composer;
  - log store merge on deferred load;
  - dead transport preference removed;
  - CI drift job split and `persist-credentials: false`.
- Five UI specs that matched source text became behavioral tests, 22 -> 40, each proven red against a planted break.
- Matt Pocock code-review: the Standards axis found no hard violations. The Spec axis found a stale error and Retry during a new send, the dead preference, CHANGELOG overclaims and a stale PRIVACY.md claim. All fixed.
- Autoreview P3, first run: one P2, where legacy key cleanup was never retried. Fixed.
- Autoreview, the one allowed rerun over the full range, found:
  - P1: a mismatched key copy whose cleanup failed could later replace the key.
  - P2: a chat purge made while stored logs were unavailable was undone by the later merge.
  - Both fixed; the legacy key now stays authoritative until the copy is confirmed. No further review run.
- Host conditions: load average 244 made react-doctor time out once, and the disk filled to 1.1 GB free, causing ENOSPC in `repo.lint.spec.ts` temp dirs. Removing this session's simulator DerivedData freed 4.7 GB, and the reruns passed.
- Device: installed the signed Release `4.4.5 (90)` on the authorized iPhone in place. It was not launched: the phone was locked, and it moved from USB to network transport, which Argent does not drive. On-device UI and runtime proof for this build are still outstanding.
