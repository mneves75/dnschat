# Release preparation

- User authorized the completed audit, version bump, all scoped changes staged/committed/pushed, beta tagging and TestFlight, followed by production promotion when ready.
- Version source bumped to 4.4.3 and sync-versions advanced both platforms to build 88; dry-run reports all manifests synchronized.
- External autoreview completed 39 passes through P3. Its one finding is INDEX-only: the old staged AAB validator accepted empty artifacts when unrelated libraries existed; the working-tree correction and behavioral tests already resolve it.
- Independent security review reproduced decrypted schema content in chat corruption-backup error metadata. Hashing that error fixes the shared sink. Both encrypted and legacy malformed-role regressions failed before the fix and passed afterward (37 focused tests).
- The staged-source review found a P3 installation regression: Git initialized with an empty template has no hooks directory. A real Git fixture reproduced exit 1; the installer now creates only the verified default hooks directory while retaining external-directory and foreign-hook protections.
- Final `verify:all` passes 136 root suites/1,010 tests and 8 native suites/68 tests; React Compiler 141/141, React Doctor 93 with zero errors and three existing warnings, Expo Doctor 19/19, audit/security/redaction and video checks pass. The final staged-source autoreview through P3 reports no accepted/actionable findings. Independent review accepts the corruption-metadata fix.
- Signed Release archive succeeds with stable Xcode 26.6 (17F113), iOS SDK 26.5. Archive metadata is 4.4.3/build 88; strict code-sign verification passes, bundled JavaScript exists, and source maps are present only as private artifacts outside the app.
- IPA export succeeds; the archive and IPA both report 4.4.3/build 88 and contain no source maps. The first commit hook exposed inherited GIT_INDEX_FILE leaking into nested Git fixtures; fixture subprocesses now discard inherited GIT_* variables. The focused script suite passes with a deliberately injected foreign index path.
- App Store Connect API authentication and distribution signing are available. Production currently reports 4.0.23; the 4.0.24 submission draft has an expired build and missing socialMedia/socialMediaAgeRestricted fields. API validation cannot verify published App Privacy. Web login requires 2FA, and the browser also has no authenticated session.
- Current physical devices are unavailable. Earlier Debug simulator/emulator proof and historical hardware proof must remain distinguished from the final distribution artifact.
- TestFlight is the configured staging lane. A manual marketing Pages workflow exists, but GitHub reports no configured Pages site; there is no existing web staging URL to verify.
