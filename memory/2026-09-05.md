# Slop cleanup and measured performance pass (Fable session)

## Situation at start

- The user asked for a slop audit, verified performance wins, and an AGENTS.md/CLAUDE.md rewrite. Three read-only auditors (tests, source/repo surface, performance) ran in parallel; every finding used below was re-verified against the code before being planned.
- A Codex session launched separately by the user was already mid-way through its own audit in the same working tree (`docs/technical/AUDIT-PLAN-2026-09.md`, shipped as `4.4.3` in `f775597`/`c5a6994`). Decision: stand down until it committed, review its diff as a contributor PR, then execute only the remainder. Its diff was reviewed in full (source, native, scripts, config): coherent, no defects found; the stagger rewrite is arithmetically identical to the old batched delays, the storage serializer change only drops a redundant Date reviver.
- What it had already done of this plan: AGENTS.md is canonical (133 lines) and CLAUDE.md imports it (14 lines); split contexts, dead animation helpers, `ClipboardService` extras, `UI/NETWORK_CONSTANTS`, `android-java17.sh`, four text specs, the native stress/integration tests.

## Track A (tests) - landed as `01b4535`

- Codex lane `slop-tests` (gpt-6-astra high) deleted 26 file-text specs, two unreferenced mocks and the per-fixture lint fan-out; merged duplicate parser coverage into `dnsPacketEncoding.test.ts` and `dnsService.spec.ts`; replaced the identity AES-GCM Jest mock with Node `aes-256-gcm` plus a tamper-rejection control (plan 016 unblocked without touching `transformIgnorePatterns`); lowered the test-discovery floor to 60 with the broken-testMatch positive control re-proven.
- Review reversed two of its cuts: the `require`/dynamic-import markdown fixtures and the member/subscript tautology fixtures are distinct AST alternatives inside the same ast-grep rules and were their only positive controls (kept one per alternative per language); the `uuid`/`xcode` CJS-loadability check guards the ESM-only-override failure this repo has hit before.
- Suite: 136 suites / 1010 tests -> 110 suites / 632 tests, native 8 / 67. Wall time unchanged (~10 s); the deletions were about value, not speed.

## Perf instrument (baseline 1, informational)

- Debug build from `~/Library/Developer/Xcode/DerivedData/DNSChat-perf` (Xcode-beta 27.0), iPhone 17 / iOS 27.0 simulator, Metro 8081 from another session (`--offline`, not stopped). Protocol from `docs/technical/SCREEN-MOUNT-PERF.md` plus two windows: send-with-Mock-DNS and startup module count.
- Host was heavily loaded by other sessions (loadavg 76-185 on 18 cores), so absolute numbers are inflated: chat thread open 55/46 ms, settings modal 137 ms (65.8 ms on the 2026-08-12 record), send window 82 ms including a 2 ms Logs-screen re-render (confirms the per-entry log fan-out). Tab taps produced no commits after the warm-up because native tabs stay mounted.
- Work-count instrument that does not depend on load: `__r.getModules()` shows react-native-udp (3) and react-native-tcp-socket (6) modules initialized at startup with only Mock DNS used. Details in `.scratch/perf/baseline-1/summary.md` (untracked).
- Lesson: `react-profiler-cpu-summary` sampled at ~13 ms per sample under this load; use commit durations and work counts, not CPU self-time, for attribution.

## In flight

- Lane `slop-src` (Track B): dead exports, 97 dead i18n keys, `@typescript/native-preview` + `ts-node` removal, AXe removal, `verify:video` out of `verify:all`, stale docs, `plans/` archive, Profile route removal, `.gitignore`, and the hardcoded pt-BR transport-throttle message bug.
- Then Track C (perf) in two parallel lanes (Codex backend, Opus frontend) measured back-to-back on the same load, then the AGENTS.md/CLAUDE.md final pass.
