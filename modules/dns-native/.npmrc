# Security-patch overrides in this workspace may reference very recent releases
# (e.g. brace-expansion >=5.0.8, js-yaml >=4.3.0, shell-quote >=1.8.5).
# Allow npm to install them immediately instead of waiting for the global
# min-release-age grace period.
min-release-age=0
