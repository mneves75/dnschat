# Installation (pointer)

Canonical install/build instructions live at `docs/INSTALL.md`.

Quick start:

```bash
bun install
bun run start
```

Build:

```bash
bun run ios
bun run android
bun run web
```

DNS smoke test:

```bash
node test-dns-simple.js "Hello world"
```
