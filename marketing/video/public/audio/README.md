# DNSChat synthetic interface sounds

These WAV files are generated by `scripts/generate-sfx.mjs` using deterministic
PCM synthesis and Node.js built-ins only. They contain no recordings, samples,
music, or third-party material. DNSChat contributors license them under the same
MIT license as this repository.

Regenerate them with:

```bash
pnpm run generate:sfx
```
